qc_page <- function() {
    fluidRow(
        box(
            title = "Quality control",
            status = "primary",
            width = 12,
            solidHeader = TRUE,
            collapsible = TRUE,
            h2("Quality control")
        )
    )
}
