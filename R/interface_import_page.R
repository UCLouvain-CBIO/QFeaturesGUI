import_page <- function() {
    fluidRow(
        box(
            title = "Import",
            status = "primary",
            width = 12,
            solidHeader = TRUE,
            collapsible = TRUE,
            h2("Import")
        )
    )
}